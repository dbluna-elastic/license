// Claim: redundancy is universal, not localized to one program. Heatmap source.
FROM tdlr-forms
| MV_EXPAND standard_fields
| STATS form_count = COUNT_DISTINCT(form_id) BY standard_fields, program_sector
| SORT form_count DESC
